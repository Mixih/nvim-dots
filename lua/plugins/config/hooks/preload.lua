-- Plugin configs that must be loaded before anything else configured. (e.g. vim-polyglot settings
-- that are loaded as soon as the filetype functionality is activated)

-- override questionable polyglot functionality
vim.g.polyglot_disabled = { 'autoindent' }
